/** Lines of TypeScript, TSX and CSS the app is built from. Counted at build time — see `vite.config.ts`. */
declare const __SOURCE_LINES__: number;

/** The build's version: how many commits are behind it. Counted at build time — see `vite.config.ts`. */
declare const __BUILD_MK__: number;
